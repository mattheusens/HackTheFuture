# Server Configuration
PORT=3000

# MongoDB Connection (local Docker)
# For local development, use: mongodb://localhost:27017
# For Azure Cosmos DB, use your connection string
MONGO=mongodb://localhost:27017

# OpenAI API (standard, not Azure)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Local Storage Configuration
# Where to store uploaded images locally
STORAGE_TYPE=local
STORAGE_PATH=./uploads

# API Base URL (for internal API calls)
# Used by image processor to call back to the API
# Default: http://localhost:3000/api
API_BASE_URL=http://localhost:3000/api
